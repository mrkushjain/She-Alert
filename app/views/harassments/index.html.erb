<h1>Listing harassments</h1>

<table>
  <tr>
    <th>Type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @harassments.each do |harassment| %>
  <tr>
    <td><%= harassment.type %></td>
    <td><%= link_to 'Show', harassment %></td>
    <td><%= link_to 'Edit', edit_harassment_path(harassment) %></td>
    <td><%= link_to 'Destroy', harassment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Harassment', new_harassment_path %>
