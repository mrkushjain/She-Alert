<%= form_for(@harassment) do |f| %>
  <% if @harassment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@harassment.errors.count, "error") %> prohibited this harassment from being saved:</h2>

      <ul>
      <% @harassment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
